import { View, Text } from '@tarojs/components';

const AddressPage = () => {
  return (
    <View className="flex flex-col h-full bg-background">
      <View className="flex flex-col justify-center items-center h-48">
        <Text className="block text-gray-400 text-sm">地址管理页面</Text>
        <Text className="block text-gray-400 text-xs mt-2">（功能开发中）</Text>
      </View>
    </View>
  );
};

export default AddressPage;